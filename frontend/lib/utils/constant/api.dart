class ApiConfig {
  // Use 'const' to make it immutable (cannot be changed at runtime)
  // Update this IP address whenever your local network changes,
  // or replace it with your production domain later.
  static const String baseUrl = "http://192.168.29.237:3000";
}
